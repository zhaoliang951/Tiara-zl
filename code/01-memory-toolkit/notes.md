# 内存四区观察笔记 · zl · 2026-09-01

> 依据：build.cmd 两次运行实测输出（2026-08-31 与 2026-09-01）
> 铁律：绝对高低由 ASLR 决定，我不记结论——只记结构与相对关系。

## 六类地址归位（今日重跑 build.cmd 的输出）

| 输出行 | 地址 | 落在哪个区 | 为什么 |
|---|---|---|---|
| code &add | 00007FF6F4F2A000 | ？ | ？ |
| .rdata string-literal | 00007FF6F4F2B730 | ？ | ？ |
| .data &g_msg_ptr | 00007FF6F4F2C000 | ？ | ？ |
| .bss &g_bss_counter | 00007FF6F4F2C898 | ？ | ？ |
| stack &stack_var | 000000D5BFE8FE94 | ？ | ？ |
| heap new#0 | 00000265FF7F6610 | ？ | ？ |

## ASLR 对比（昨天 vs 今天）

- 变了的：？（哪些区域、怎么变的）
- 没变的：？（顺序？开头？为什么不会乱）

## 四个 depth 地址逐位抄录

- depth=1:
- depth=2:
- depth=3:
- depth=4:
- 它们严格递减吗？答：？

## 两个挂账追问

1. 为什么"复用空闲块"导致堆地址无序？答：？
2. 怎么反驳"栈向下是物理定律"？答：？

## Debug 堆 vs Release 堆（D20）
1. 在Debug堆中，内存块被free释放之后，**不会立刻复用这块内存**，还会把释放出来内存全部填充上`0xDD`标记。如果程序不小心读写已经释放掉的内存，就会碰到这个特殊标记，很容易就暴露“使用已释放内存”这个bug。
2. Debug堆给每一块用户内存的左右两侧，额外设置了警戒线（no‑man's land，无人区）。一旦代码发生越界读写，就会破坏警戒线，调用free的时候就触发报警，检测缓冲区越界问题。
3. 二者设计目标不一样：**Debug堆的首要目标是抓bug（抓虫），牺牲一部分运行效率；Release堆优先追求运行速度，做性能优化。两套堆拥有完全不一样的行为特性。

>（加分项，自己实验的联系）我做实验的时候，D19那5块内存释放完，再new出来得到的是全新地址，旧内存不会拿来重复分配，根源就是Debug堆不会马上复用释放后的内存块。
## cookie 何时存在（D21）

编译器需要挨个调用析构的时候，才安排 cookie；不需要挨个析构，就不要 cookie。不是堆运行时去查数量，很多信息编译期就定好了。
