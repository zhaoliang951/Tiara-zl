# 费曼讲解 · 一个 C 程序的内存里都住着什么

> zl ｜ W1-D4 ｜ 听众假设：零基础
> 规矩：比喻开头，实测证据跟上；术语出现必须当场解释。

## 开场：内存是一栋楼，地址是门牌号

？（填：内存=楼、地址=门牌号，然后引出"这栋楼每晚还会换门牌"——把 ASLR 放结尾更震撼，或放开场自己定）

## 1. 栈：自动租住，自动退房

“自动租住自动退房” + “楼下加盖新房，实测每次减 64”（你的原话：进入函数=分配房间，函数结束=清空回收；只允许从顶端存取=后进先出）

我们连跑 4 层递归，门牌号每次刚好减 64——CFFC20 → CFFBE0 → CFFBA0 → CFFB60。数字会变（ASLR），但“逐层变小”永远不变。（补实测：每次函数调用=楼下加盖新房，越深门牌号越小——我们实测每次刚好减少 64 个编号）

## 2. 堆：手动申请的商铺

商铺转租” + “连续申请 5 块门牌号乱序” + “忘记归还=泄漏”（你的原话：手动申请、必须归还、门牌号=指针、忘记归还=内存泄漏、空间大但稍慢）

heap new#0 ~ new#4 五个乱序地址 + heap strictly increasing? = NO    五个地址都是乱序（补实测：新商铺未必排在上一间后面——退租的铺子会转租；实测连租 5 块，门牌号乱序）

## 3. 全局区：两间格子

“.data 开工摆好物品 / .bss 空房系统填零” + 指针和计数器的例子（你的原话：.data 开工摆好物品，.bss 空房启动填零；例子：全局字符串指针住 data，未初始化计数器住 bss）

## 4. 常量区：只读

“只读强写崩溃” / “机器指令，CPU 逐条读取”（你的原话：双引号文字住这，只读，强写即崩）

## 5. 代码区：大楼最底层

“只读强写崩溃” / “机器指令，CPU 逐条读取”（你的原话：机器指令，CPU 逐条读取运行，同样只读）

## 结尾：这栋楼每晚都换门牌（ASLR）

“整楼每晚换门牌，距离不变，差值验证=整体搬迁”（你的原话：整楼换门牌防黑客按图索骥，但房间之间距离固定不变——我们用两天的数据差值验证过"整体搬迁"）

## 附：我的知识空洞清单（2026-09-02）

1. 指针为什么固定 8 字节 —— 状态：？（任务 3 做完后改成"已补"）因为指针由系统寻址位数觉定
2. 栈帧里除了局部变量还住什么（返回地址） —— 待补（W11/W13）
3. bss 为什么启动填零而不存文件里 —— 待补（W9）
4. 为什么栈快堆慢 —— 待补（W4）
5. exe 和 DLL 的区别 —— 待补（W8）
6. 什么是线程、栈是每线程一条吗 —— 待补（W4.5）
