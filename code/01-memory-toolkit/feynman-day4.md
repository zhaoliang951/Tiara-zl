# 费曼讲解 · 一个 C 程序的内存里都住着什么

> zl ｜ W1-D4 ｜ 听众假设：零基础
> 规矩：比喻开头，实测证据跟上；术语出现必须当场解释。

## 开场：内存是一栋楼，地址是门牌号

程序运行时的一切——指令、文字、变量——都住在内存这栋大楼里。今天带你从下往上逛一圈：这栋楼里都住着谁。

第一个房间住着栈，栈就像是大楼高层的临时公寓，规则是**自动租住，自动退房**。
每当进入一个函数，就相当于在楼下加盖一间新房；函数执行结束，这间房间就立刻清空回收。而且公寓只允许从最顶端存取物品，也就是后进先出。
## 1. 栈：自动租住，自动退房

“自动租住自动退房” + “楼下加盖新房，实测每次减 64”
我们连跑 4 层递归，门牌号每次刚好减 64——CFFC20 → CFFBE0 → CFFBA0 → CFFB60。数字会变（ASLR），但“逐层变小”永远不变。
## 2. 堆：手动申请的商铺

堆相当于大楼里一片商业街商铺，**商铺不能自动分配，必须手动申请，用完也必须主动归还**。
拿到的商铺门牌号，就是我们所说的**指针**。如果申请了商铺，用完却忘记归还，就会一直被占用，也就是**内存泄漏**。堆的空间很大，但读写会比栈稍慢一点。

heap new#0 ~ new#4 五个乱序地址 + heap strictly increasing? = NO    五个地址都是乱序     实测结果并不固定：有时 NO，有时 YES——分配器不承诺任何顺序，能依赖的只有‘不保证’。

## 3. 全局区：两间格子

全局区拆成两块，一间叫`.data`区，一间叫`.bss`区。

- `.data`区：开工时就摆好物品，存放**已经赋好初始值**的全局变量。
- `.bss`区：是空房间，程序启动时系统自动把里面全部填零，存放**没有初始化**的全局变量。

举个例子：全局的字符串指针存放在`.data`；未初始化的全局计数器，就放在`.bss`。
## 4. 常量区：只读

常量区存放用双引号写出来的文字常量。
这片房间有硬性规定：**只允许读，不允许改写**，一旦强行修改里面的内容，程序就直接崩溃。
## 5. 代码区：大楼最底层

大楼最底层是代码区。这里放着编译好的机器指令，也就是我们写的 C 代码翻译成机器能读懂的命令，同样是只读的。CPU 会逐条读取这里的指令，然后执行

## 结尾：这栋楼每晚都换门牌（ASLR）

最后回到开头的伏笔：操作系统每次启动程序，都会把整栋大楼所有房间的门牌号整体重新随机更换一遍，用来防止黑客利用固定地址攻击程序。
但是，各个房间之间的相对距离永远不变，也就是我们做差值验证看到的 “整体搬迁” 现象。
## 补充：教科书的图 vs 我的实测

教科书画的‘代码在最底、栈在最高’是经典模型；我们实测 Windows 上模块区反而在最高位——位置由 ASLR 决定
① 教科书画"代码在最底、栈在最高"——那是经典/Linux 模型
② 我们实测：栈 ≈150GB、堆 ≈266GB、模块区 ≈140TB——Windows 上模块区在最高位
③ 结论：区域位置由 ASLR 决定，我不记结论，只记"模块区内部顺序固定"
## sizeof 家族实测（W2）
成员间垫字节，是为了结构体内部每个成员对齐；尾部凑整，是为了结构体数组里每个元素都对齐。两处 padding，同一个目的：让对齐承诺在任何摆放方式下都成立。
| 类型/结构体 | 手算 | 实测 |
|---|---|---|
| struct S (char,int,char) | 12 | 12 |
| struct Inner (char,int) | 8 | 8 |
| struct Outer (char,Inner,char) | 16 | 16 |
| struct T (char,double,int[3]) | 32 | 32 |
| int* / char* / void* | 8 | 8 |

手算与实测全部一致。padding 不是浪费，是 CPU 对齐读取的代价。

## bss 为什么不存 0（空洞 #3 已补）

exe 只记录 bss 区的位置和大小，不存储那些 0——100 万个未初始化全局变量只多占几字节文件；加载时由操作系统统一把整块内存清零。
## 虚函数表实证（W3）
vptr 住对象开头：父类指针拿到首地址就能找到子类的虚表（多态的根基）。
vtable 住只读区：编译期常量，OS 只读保护防篡改。
跨运行对比：vtable 低 16 位（13F0/13D0）恒定，只有基址漂移——整体搬迁再实证。
## 对象内存布局图（W3）

图中四个地址全部来自同一次运行（2026-09-16）。

┌─ 栈区（低地址，0x000000B4）──────────────────────┐
│  Animal a                                        │
│  +0  vptr = 00007FF7737F7690 ────────────────────┼──┐
│  +8  legs = 4（&legs = 000000B47F7FFE70）          │  │
└──────────────────────────────────────────────────┘  │
                                                      ▼
┌─ 模块区 .rdata（高地址，0x00007FF7）─────────────┐
│  vtable                                          │
│  [0] = 00007FF7737E13F0 ──────────────────┐      │
│  [1] = 00007FF7737E13D0 ────────────┐     │      │
└─────────────────────────────────────┼─────┼──────┘
                                      ▼     ▼
┌─ 模块区 .text ──────────────────────────────────┐
│   speak() 的机器码         eat() 的机器码         │
└─────────────────────────────────────────────────┘

前面我们逛完了这栋楼的房间布局。现在换个角度问一个问题：C++ 的对象在这栋楼里怎么住？ 答案从每个带虚函数的对象开头那 8 个字节说起。
- 对象开头 8 字节是 vptr，实测指向模块区 .rdata（只读）
- 虚表内容 = 函数指针数组，按声明顺序排列：实测 vtable[0]=speak(…13F0)、vtable[1]=eat(…13D0)
- 表里的指针指向 .text 代码段——虚表住只读数据区，表里的地址指代码区
- 手动经 vptr 调用：fn = vtable[0]，fn(&a) 输出 woof——没有写 a.speak() 也调到了
- 实测（MSVC 19.44）：vtable[1] = eat 地址，非 nullptr——**MSVC 虚表无哨兵**；GCC 未测，不置可否
- 虚函数调用底层：取 vptr → 查虚表 → 调表项，并非编译期固定函数地址

## 附：我的知识空洞清单（2026-09-02）
1. 指针为什么固定 8 字节 —— 已补（2026-09-07）：实测三指针全 8
2. 栈帧里除了局部变量还住什么（返回地址） —— 已补（2026-09-18）：call 压入的"回家的路"，住局部变量上方（高地址侧）；栈高→低长、写入低→高延伸，缓冲区溢满即覆盖返回地址，ret 时跳向攻击者地址——S4 攻击面总地基。防御：ASLR/Canary/DEP/CFG。（来源：豆包）
3. bss 为什么启动填零而不存文件里 —— 已补（2026-09-09）：exe 只记大小不存 0，加载时 OS 清零
4. 为什么栈快堆慢 —— 已补（2026-09-08）：栈=硬件移指针，堆=内核态查空闲块
5. exe 和 DLL 的区别 —— 已补（2026-09-10）：exe 独立运行有入口点，DLL 被加载供调用
6. 线程与线程栈 —— 已补（2026-09-11）：一人一条栈（1MB 保留，凭记忆待核实），共享堆/全局，私有栈/寄存器

---

## W4：new/delete 与内存泄漏（2026-09-25）

### 1. new 和 delete 各做两件事

`new` 一共做两件事：第一件，去堆内存里申请一块足够大小的原始内存；第二件，在刚拿到的这块内存上运行**构造函数**——构造函数就是用来初始化对象成员变量的函数。

`delete` 也做两件事：第一件，调用对象的**析构函数**，负责清理对象内部占用的资源；第二件，把这块堆内存还给系统的内存池，让这块内存可以被后续的分配再次使用。

**实验证据 1（9/22，leak.cpp）**：只 new 不 delete，`_CrtDumpMemoryLeaks` 输出 `{160} normal block at ... , 8 bytes long`。`{160}` 是分配序号——程序运行中第 160 次分配的堆块；8 bytes 是真实体积（2 个 int）。分配序号 ≠ 字节大小。

**实验证据 2（9/25，array_cookie.cpp）**：给 Player 加打印构造函数后实测——`new Player[5]` 时 5 行 `Player constructed` 立刻打出，`delete[]` 时 5 行 `~Player` 才打出。分配→构造→析构→释放的顺序是屏幕上看出来的，不是背的。

### 2. 泄漏检测全链路（D19-D20 亲手走的路）

1. `#define _CRTDBG_MAP_ALLOC` 必须写在所有 include **之前**——让调试库把分配记录绑定到源码行号，放后面宏不生效，定位不了泄漏行。
2. 重定向三行（`_CrtSetReportMode`/`_CrtSetReportFile`）——报告默认走调试通道，控制台直接跑看不见；没看见 ≠ 没泄漏。
3. 故意不 delete → 报告点名 `{160}` 号块 8 bytes，Data 里躺着 hp=100、level=1——分配了没归还，被点名。
4. `_CrtSetBreakAlloc(160)` + F5 → 程序停在第 160 次分配瞬间，调用堆栈定位到 main() 第 18 行。注意：**序号是构建环境的属性**，换了工程就要重新看报告里的 {n}。
5. delete 前置 → 零泄漏。检测函数是**拍照快照**：先还钱，再拍照。

### 3. new[]/delete[] 与 cookie

cookie 是编译器在数组堆内存头部额外开辟的隐藏小块，用来记录数组元素数量。

**核心规则：只有类型带自定义析构函数，new[] 分配数组时才生成 cookie。**

- A 组：`new Player[5]`（有析构）→ cookie = 5；`delete[]` 读 cookie 知道要调 5 次析构，输出 5 行 `~Player`——cookie 干活的直接证据。
- B 组对照：`new Monster[5]`（无析构）→ 同位置读到 `0xFDFDFDFD0000009F`：高 32 位是 Debug 堆警戒线，低 32 位是请求号 159。**没有 cookie 的铁证**——我预测是 5，实验说没有，认知被自己的数据修正。
- 对比结论：同样 new[5]，有无自定义析构函数决定 cookie 是否存在。

### 4. Debug 堆 vs Release 堆（一句话版）

Debug 堆填魔数、设警戒边、加 cookie 校验，为了抓越界和内存损坏；Release 堆去掉全部调试填充，轻量化、求快。两套堆两个性格：一个求抓虫，一个求速度。

### 周会自测两题（自答）

**① 从发现泄漏到定位到行的完整流程？**
定义宏 + 重定向输出 → 只 new 不 delete 跑出报告拿到分配序号 → `_CrtSetBreakAlloc(序号)` + F5 断点 → 调用堆栈定位到分配内存的源码行。

**② new Player[5] 和 new Monster[5] 各分多少字节？**
Player 单个 8 字节：5×8 + cookie 8 = **48 字节**；Monster 无析构无 cookie：**40 字节**。差在 cookie：Player 需要记录数量供 delete[] 循环析构，Monster 不需要。
）
